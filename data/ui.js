// Colors for the projects previews
const colors = ["light", "dark", "primary", "secondary"];

// Corner identifications for rounding project previews corners
const corners = ["top_left", "top_right", "bottom_left", "bottom_right"];

module.exports = {
  colors,
  corners,
}